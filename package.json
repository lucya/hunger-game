{
  "name": "hunger-game",
  "version": "1.0.0",
  "description": "음식 토너먼트 게임",
  "main": "script.js",
  "scripts": {
    "start": "npx http-server . -p 8080",
    "dev:workers": "wrangler dev --port 8787 --local",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "food-tournament",
    "game",
    "javascript",
    "html",
    "css",
    "cloudflare-workers"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
